void main() {
    int x;
    read(x);
    switch (x) {
        1 : { print(20); }
        2 : { print(40); }
        3 : { print(60); }
        4 : { print(80); }
        5 : { print(100); }
        default : { print(0); }
    }
}
