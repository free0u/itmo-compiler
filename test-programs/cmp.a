int cmp(int a, int b) {
    if (a < b) {
        return -1;
    } else {
        if (a == b) {
            return 0;
        } else {
            return 1;
        }
    }
}

void main() {
    int x;
    int y;
    read(x);
    read(y);
    print(cmp(x, y));
}
